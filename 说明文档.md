每个页面（功能）都由三个组件组成：

预览组件：生成的图片/视频结果预览区域；
历史记录组件：生成的图片/视频历史记录列表；
参数设置组件：生成图片/视频的参数设置（例如分辨率，模型版本（base、pro）、上传图片、提示词、比例等等）；
生成按钮组件：生成视频的按钮；

要求：所有的页面（pages目录里的页面）共用（复用）：预览组件、历史记录组件、参数设置组件、生成按钮组件。

另外需注意，其中有一个特殊的地方是veo-studio生成的是视频，而不是图片，如果资源是视频，则在预览组件和历史记录组件需考虑兼容性问题并添加播放按钮。
